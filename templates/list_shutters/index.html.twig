{% extends 'base.html.twig' %}

{% block title %}Control center{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Liste des volets</h1>

    <ul>
    {% for shutter in shutters %}
        <li>{{ component('ShutterControl', { 
            id: shutter.id,
            label: shutter.label,
            siteId: shutter.siteId 
        }) }}</li>
    {% endfor %}
    </ul>
</div>
{% endblock %}
